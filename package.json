{
  "name": "klem-site",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "turbo run build --filter=showcase-website",
    "dev": "turbo run dev --filter=showcase-website"
  },
  "devDependencies": {
    "turbo": "^2.9.18"
  }
}
