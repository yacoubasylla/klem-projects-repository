spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/klem_core
    username: core_api
    password: core_api
  jpa:
    hibernate:
      ddl-auto: validate
  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    com.klem.coreapi: DEBUG
