package com.klem.cantine.eleve.entity;

/**
 * Périodicité de l'abonnement cantine (mode ABONNEMENT). Pas de mensualisation —
 * règle métier : paiements trimestriels ou annuels uniquement.
 */
public enum PeriodeAbonnement {
    TRIMESTRIEL, ANNUEL
}
