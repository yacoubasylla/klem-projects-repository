# Assets manquants (à fournir avant le premier build)

`app.json` référence `icon.png`, `splash.png` et `favicon.png` dans ce dossier —
volontairement non générés ici (pas d'images fabriquées sans direction visuelle
réelle). Utiliser les mêmes visuels que le web
(`client-frontend/src/assets` côté web-app) ou la charte KLEM
(`platform-devsecops/templates`) une fois choisis.
